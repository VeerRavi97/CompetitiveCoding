//Typenames

typedef long long ll;
typedef vector <ll >     vi;
typedef pair <ll,ll> ii;
typedef vector < ii > vii;
//

// Define Macros 
#define N 1000001
#define MINF -0x3f3f3f3f
#define INF 0x3f3f3f3f
#define all(c) c.begin(),c.end()
#define del cout << "\n"
//



// Nullify
fill(all(v),0);
memset(arr,0,sizeof(arr));
//
don't write shortening codes for scanf,printf, for loop,push_back,first...etc as this will mess up your code
